# sshore
Terminal-native SSH connection manager and SFTP client built in Rust. Color-coded environment tiers (prod/staging/dev), terminal tab theming, persistent tunnels with auto-reconnect, bookmark CRUD, sudo password assist, and fuzzy search. Single binary, zero config to start.
